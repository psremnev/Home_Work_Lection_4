<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- checked -->
    <item android:state_checked="true" android:drawable="@color/success"/>
    <!-- unchecked -->
    <item android:drawable="@color/danger"/>
</selector>